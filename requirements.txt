# MNEMOS production dependencies
# Generated 2026-04-10 — reflects actual imports

# API framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.40.0
pydantic>=2.8.0
python-multipart>=0.0.9

# Database
asyncpg>=0.29.0          # async API path (lifecycle.py + handlers)
psycopg[binary]>=3.1.0   # sync background tasks (core.py, export scripts) + distillation worker

# HTTP client (single library — replaces requests + aiohttp)
httpx>=0.27.0

# Cache
slowapi>=0.1.9
limits>=3.6.0
redis>=5.0.0

# Memory/config
python-dotenv>=1.0.0

# MCP server
mcp>=1.0.0

# Async utilities (transitive, pinned for stability)
anyio>=4.4.0

# Utilities
numpy>=1.26.0
psutil>=5.9.0

# Optional: Intel OpenVINO / phi_server.py (local GPU inference)
# NOTE: PROTEUS has NO GPU hardware. Use CERBERUS (192.168.207.96, RTX 4500 Ada) for GPU workloads.
# Install on GPU systems only: pip install mnemos-os[phi]
# openvino-genai>=2024.4.0
# fastembed>=0.3.0

# Optional: Full SENTENCE compression support (sentence-level compression)
# sentence-transformers>=2.7.0
# spacy>=3.7.0

# Optional: Full extractive token filter compression support
# networkx>=3.3
