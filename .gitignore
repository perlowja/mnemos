# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/

# Secrets & API keys (never commit)
.env
.env.*
*_secrets.py

# Backup files
*.bak
*.bak.*
*.backup
*.orig

# Logs & runtime data
*.log
*.db
*.sqlite3

# Audit / report files
*_AUDIT_*.txt
*_AUDIT_*.md

# IDE
.vscode/
.idea/
*.swp

# Ops scripts — internal infrastructure only
deploy.sh
git_sync_daily.sh
run_monthly_export.sh
monitor_distillation.sh
sync_argonas_git.py

# Internal audit/ops artifacts
ARGONAS_*
NFS_*
*_AUDIT_*.txt
*_FIXES_*.txt
*_COMPLETE_*.txt
*_SUMMARY_*.txt
*_REPORT_*.txt
OPTIMIZATION_*

# Development history — internal only
docs/archive/

# Model weights — download separately, never commit
models/

# Runtime / generated
venv/
data/
.pytest_cache/

# Internal/inactive feature code — not for public distribution
contrib/
export_to_json_shards.py

# Local env and secrets
.env
.env.*

# Local logs and reports
api.log
*_REPORT_*.txt
*_FIXES_*.txt

# Local backup files
*.bak
*.bak.*

# Local pytest and caches
.pytest_cache/

# Internal repo layout notes for ops clones only
*.stale_*
!.env.example

# Build artifacts
*.egg-info/
dist/
build/
__pycache__/

# Local scripts not for distribution
sync_argonas_git.py
git_sync_daily.sh
run_monthly_export.sh
monitor_distillation.sh
deploy.sh

# Runtime configuration (contains secrets — use config.toml.example as template)
config.toml

# API key files (never commit actual keys)
api_keys.json
